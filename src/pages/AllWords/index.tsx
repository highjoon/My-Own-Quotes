import WordList from "@/components/WordList";
import React, { FC, Fragment } from "react";

const AllWords: FC = () => {
  return (
    <Fragment>
      <WordList />
    </Fragment>
  );
};

export default AllWords;
